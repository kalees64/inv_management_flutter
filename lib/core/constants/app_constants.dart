class AppConstants {
  static const String appName = 'Custom ERP & Odoo';

  // Api Constants
  // Use localhost for Android emulator (10.0.2.2) or local IP for devices
  // For web, localhost:3000 works if json-server is running locally
  static const String baseUrl = 'http://localhost:3000';

  static const String keyToken = 'auth_token';
  static const String keyUser = 'user_data';
}
